node_modules
dist
.DS_Store
*.local
.env
.env.local
.dev.vars
.wrangler
*.tsbuildinfo
prompt.log.jsonl
.claude

# Playwright — committed: e2e/__screenshots__ (baselines). Ignored: run output.
playwright-report
test-results
e2e/.cache

# Local scratch — preview server log + throwaway screenshot scripts.
*.log
shot*.mjs
